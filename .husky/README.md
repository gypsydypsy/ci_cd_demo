HUSKY permet de gérer les git hooks

git hooks = machine locale !== github actions = serveur / cloud

pour initialiser husky 
- npm install --save-dev husky
- npx husky init
- configurer les hooks dans package.json

travail avec lint-staged = permet de vérifier uniquement les fichiers stagged 